@model OrderViewModel

@{
    ViewData["Title"] = "CreateOrder";
}

<h3>Форма оформления покупки</h3>
<p>Для оформления покупки заполните следующие поля:</p>
<div class="container">
    <div class="row">
        <form asp-action="CreateOrder" asp-controller="Order">
            <div class="col">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="text-black-50">
                    <label asp-for="Name" class="control-label">Имя</label>
                    <input type="text" asp-for="Name" value="@Model.User.UserData.Name" class="form-control" />
                    <span asp-validation-for="Name" class="text-danger"></span>
                </div>
                <div class="text-black-50">
                    <label asp-for="City" class="control-label">Город</label>
                    <input type="text" asp-for="City.city" value="@Model.User.UserData.City.city" class="form-control" />
                    <span asp-validation-for="City" class="text-danger"></span>
                </div>
                <div class="text-black-50">
                    <label asp-for="Address" class="control-label">Адрес</label>
                    <input type="text" asp-for="Address" class="form-control" />
                    <span asp-validation-for="City" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="Comment" class="control-label">Комментарий</label>
                    <input type="text" asp-for="Comment" class="form-control" />
                    <span asp-validation-for="Comment" class="text-danger"></span>
                </div>
                <div class="text-black-50">
                    <label asp-for="Date" class="control-label">Дата</label>
                    <input type="text" asp-for="Date" value="@Model.Date" class="form-control" />
                    <span asp-validation-for="Date" class="text-danger"></span>
                </div>
            </div>
            <div class="col">
                <div class="text-black-50">
                    <label asp-for="cart.Cartlines" class="control-label">Продукты</label>
                    @foreach (var p in Model.cart.Cartlines)
                    {
                        <div class="row">
                            <div class="col">
                                Название
                            </div>
                            <div class="col">
                                Количество
                            </div>
                            <div class="col">
                                Цена
                            </div>
                        </div>
                        <div class="row">
                            <div class="col">
                                @p.Product.PName
                            </div>
                            <div class="col">
                                @p.Quantity
                            </div>
                            <div class="col">
                                @p.TotalCostProducts.ToString("# грн")
                            </div>
                        </div>}
                </div>
                <div class="text-black-50">
                    <h3><label class="control-label"></label>Итоговая цена</h3>
                    <div>@Model.cart.TotalCostAllProducts.ToString("# грн")</div>
                </div>
                <div>
                    <input type="hidden" asp-for="cart.ShoppingCartId" value="@Model.cart.ShoppingCartId" />
                    <input type="hidden" asp-for="User.UserId" value="@Model.User.UserId" />
                </div>
                <div>
                    <input type="submit" value="Оформить" class="btn btn-success" />
                </div>
            </div>
        </form>
    </div>
</div>